import { KeyboardEvent } from 'react';
import { Config, DataGridNav } from '@table-nav/core';

declare function useTableNav(options?: Config): {
    listeners: {
        onKeyDown: (e: KeyboardEvent) => void;
        onKeyUp: () => void;
    };
    tableNav: DataGridNav;
};

export { useTableNav };
